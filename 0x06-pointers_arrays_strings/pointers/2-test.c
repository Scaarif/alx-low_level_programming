#include <stdio.h>

int main(void)
{
	int *p; /*declare a pointer to a int type variable*/

	printf("Size of pointer: %lu\n", sizeof(p)); /*get the size of the pointer; use sizeof () */
	return (0);
}

